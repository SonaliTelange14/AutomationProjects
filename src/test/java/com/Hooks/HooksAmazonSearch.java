package com.Hooks;

import io.cucumber.java.After;
import io.cucumber.java.Before;
import static com.StepDefination.S06AmazonSearch.driver; //import webdriver from S06AmazonSearch class

import java.time.Duration;

import org.openqa.selenium.chrome.ChromeDriver;

public class HooksAmazonSearch {
	/*
	 * @Before public void launchBrowser() {
	 * 
	 * driver = new ChromeDriver(); driver.manage().window().maximize();
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
	 * 
	 * driver.get("https://www.amazon.in/"); }
	 * 
	 * @After public void closeBrowser() { driver.close(); }
	 */

}
